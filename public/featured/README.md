# Featured Images

此文件夹用于存放 `app/page.tsx` 中 featured 卡片使用的图片。

当前期望的文件名：

* `report-1.png`
* `report-2.png`
* `chips-1.png`
* `chips-2.png`

如果你想用不同的文件名或格式（如 `.jpg`、`.webp`），需要在 `app/page.tsx` 里的 `FEATURED_SHOWCASE` 中修改对应路径。

路径格式示例：

* `/featured/report-1.png`
